    enum WhereOperator {
        AND
        OR
        OPTIONAL
    }

    input Constraint {
        operand: String!
        operator: String!
        expression: String
    }

    input WhereClause {
        operator: WhereOperator
        constraints: [Constraint]
    }
